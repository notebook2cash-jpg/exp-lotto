name: Run Exp Lotto Stats

on:
  workflow_dispatch: {}
  schedule:
    - cron: "0 1 * * *"   # ทุกวัน 08:00 น. ไทย (01:00 UTC)

permissions:
  contents: write
  models: read

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Run script
        env:
          # token pool หลายตัวจาก Secrets
          GITHUB_TOKENS: ${{ secrets.MODELS_TOKENS }}

          # โหมดอ่านใหม่ทั้งหมด
          FULL_REFRESH_MODE: "1"
          FULL_REFRESH_MAX_WAIT_MINUTES: "5"

          # โหมดสปีดสูง (เพิ่งเพิ่มในโค้ด)
          FAST_RUN_MODE: "1"
          FAST_RUN_MAX_MINUTES: "5"

          # ลดช่วงห่างให้เร็วขึ้น
          MIN_AI_INTERVAL_MS: "300"

          # ไม่ให้ติดเพดาน call ในรอบ
          MAX_AI_CALLS_PER_RUN: "200"
        run: node scripts/scrape-calculation.mjs

      - name: Commit & push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes."
            exit 0
          fi
          git commit -m "chore: update exp lotto stats"
          git push
